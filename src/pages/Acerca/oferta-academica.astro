---
import Layout from "@/layouts/Layout.astro";
import Instalaciones from "@/assets/Imagenes/CardOferta/Instalaciones.png";
import Niveles from "@/components/sections/acerca/Niveles.astro";
---

<Layout>
  <main class="min-h-screen">
    <!-- Hero Section -->
    <section
      class="oferta-bg relative min-h-[65vh] overflow-hidden bg-cover bg-center bg-no-repeat"
    >
      <!-- Desktop Background -->

      <!-- Hero Content -->
      <div
        class="relative z-10 container mx-auto px-4 h-full min-h-[65vh] flex flex-col md:flex-row items-center justify-center md:justify-end"
      >
        <div
          class="w-full max-w-7xl flex flex-col md:flex-row items-center justify-between"
        >
          <!-- Left side - FAQ Title -->
          <div
            class="flex items-center space-x-4 mb-6 md:mb-0 md:space-x-8 text-white"
          >
            <div
              class="w-16 h-16 bg-white bg-opacity-20 rounded-full flex items-center justify-center"
            >
            </div>
          </div>

          <!-- Right side - Welcome message -->
          <div
            class="bg-white rounded-lg p-6 max-w-sm shadow-lg text-center md:text-left"
          >
            <h1 class="text-xl font-bold text-gray-800 mb-3">
              Descubre tu futuro profesional
            </h1>
            <p class="text-gray-600 text-sm leading-relaxed">
              Encuentra el programa educativo perfecto para ti, con múltiples
              modalidades y reconocimiento oficial
            </p>
            <a>INICIAR INSCRIPCIÓN</a>
          </div>
        </div>
      </div>
    </section>

    <section class="bg-white py-16 px-4 sm:px-6 lg:px-8">
      <div class="max-w-7xl mx-auto">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <!-- Contenido de texto -->
          <div class="space-y-6">
            <h2
              class="text-4xl md:text-5xl font-bold text-gray-900 leading-tight"
            >
              Conoce todo lo que UNE
              <br />
              <span class="text-azul-une">tiene para ti</span>
            </h2>

            <p class="text-lg text-gray-600 leading-relaxed">
              En UNE tenemos todo para ti. Ofrecemos distintos niveles
              educativos y diversas modalidades que se adaptan a tus
              necesidades, para que elijas la opción que más te convenga.
              Además, nuestros programas están respaldados por planes de estudio
              reconocidos, como los de la Universidad de Guadalajara (UDG) y la
              Secretaría de Innovación, Ciencia y Tecnología (SICYT).
            </p>
          </div>

          <!-- Imagen -->
          <div class="relative">
            <div class="relative overflow-hidden rounded-2xl shadow-2xl">
              <img
                src={Instalaciones.src}
                alt="Instalaciones UNE - Laboratorio moderno con equipos especializados"
                class="w-full h-auto object-cover"
                loading="lazy"
              />

              <!-- Overlay decorativo opcional -->
              <div
                class="absolute inset-0 bg-gradient-to-tr from-blue-600/10 to-red-600/10 pointer-events-none"
              >
              </div>
            </div>

            <!-- Elementos decorativos -->
            <div
              class="absolute -top-4 -right-4 w-24 h-24 bg-blue-100 rounded-full opacity-60"
            >
            </div>
            <div
              class="absolute -bottom-6 -left-6 w-32 h-32 bg-red-100 rounded-full opacity-40"
            >
            </div>
          </div>
        </div>
      </div>
    </section>

    <Niveles/>

   
  </main>
</Layout>

<style>
  .oferta-bg {
    background-image: url("/src/assets/Imagenes/Heros/Oferta.png");
  }

  @media (max-width: 767px) {
    .oferta-bg {
      background-image: url("/src/assets/Imagenes/Heros/Oferta.png");
    }
  }
</style>
