---
import { Shield, Award, Lightbulb } from "@lucide/astro";
import Fondo from "@/assets/Imagenes/Middles/Fondo-plantel.png";


---

<section class="relative h-96 md:h-[500px]  overflow-hidden bg-cover bg-center"
  style={{ backgroundImage: `url(${Fondo.src})` }}

>
  <!-- Background image (you can replace with your actual image) -->
  <div class="absolute inset-0 bg-[url('/path-to-your-background-image.jpg')] bg-cover bg-center bg-no-repeat"></div>
  
  <!-- Content -->
  <div class="relative z-10 container mx-auto px-4 py-12 flex flex-col items-center justify-center h-full text-center">
    
    <!-- Top badge -->
    <div class="mb-8">
      <span class="inline-block bg-blue-600/20 border border-blue-400/30 rounded-full px-6 py-2 text-sm text-blue-100 backdrop-blur-sm">
        Disponible para toda la comunidad universitaria
      </span>
    </div>
    
    <!-- Main heading -->
    <h1 class="text-2xl md:text-3xl lg:text-4xl font-bold text-white mb-4 leading-tight">
      ¿Listo para impulsar tu aprendizaje?
    </h1>
    
    <!-- Subtitle -->
    <p class="text-base md:text-lg text-blue-100 mb-8 max-w-2xl leading-relaxed">
      Envíanos un mensaje y descubre cómo podemos ayudarte a transformar tus proyectos académicos en realidad.
    </p>
    
    <!-- Features -->
    <div class="flex flex-col md:flex-row gap-6 mb-8">
      <div class="flex items-center gap-3 text-white">
        <div class="w-10 h-10 bg-white/10 rounded-full flex items-center justify-center backdrop-blur-sm">
          <Shield class="w-5 h-5" />
        </div>
        <span class="text-sm font-medium">Seguridad</span>
      </div>
      
      <div class="flex items-center gap-3 text-white">
        <div class="w-10 h-10 bg-white/10 rounded-full flex items-center justify-center backdrop-blur-sm">
          <Award class="w-5 h-5" />
        </div>
        <span class="text-sm font-medium">Calidad</span>
      </div>
      
      <div class="flex items-center gap-3 text-white">
        <div class="w-10 h-10 bg-white/10 rounded-full flex items-center justify-center backdrop-blur-sm">
          <Lightbulb class="w-5 h-5" />
        </div>
        <span class="text-sm font-medium">Innovación</span>
      </div>
    </div>
    
    <!-- CTA Button -->
    <button class="bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-6 rounded-lg text-base transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
      ¡Comienza ahora!
    </button>
    
  </div>
  
  <!-- Decorative elements -->
  <div class="absolute top-20 left-10 w-32 h-32 bg-blue-400/10 rounded-full blur-xl"></div>
  <div class="absolute bottom-20 right-10 w-40 h-40 bg-blue-300/10 rounded-full blur-xl"></div>
</section>

<style>
  /* Custom CSS for bg-rojo-une if you want to define it */
  .bg-rojo-une {
    background-color: #dc2626; /* You can replace this with your specific UNE red color */
  }
</style>