---
import FondoAccesos from "@/assets/Imagenes/Adaptables/Ilustracion_clara.jpg";
import FondoAccesos_oscuro from "@/assets/Imagenes/Adaptables/Ilustracion_oscura.jpg";


---

<section class="py-12 px-4">
  <div class="max-w-6xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <!-- Control Escolar -->
      <div class="group">
        <a 
          href="/control-escolar" 
          class="block relative overflow-hidden rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl"
        >
          <div 
            class="absolute inset-0 bg-cover bg-center bg-no-repeat"
            style={`background-image: url(${FondoAccesos.src})`}
          ></div>
          <div 
            class="absolute inset-0 opacity-0 dark:opacity-100 bg-cover bg-center bg-no-repeat transition-opacity duration-300"
            style={`background-image: url(${FondoAccesos_oscuro.src})`}
          ></div>
          <div class="relative p-8 h-64 flex flex-col justify-between">
            <div class="border-t-2 border-white mb-4"></div>
            <div class="flex-grow flex items-center justify-center">
              <h3 class="text-2xl font-bold text-white text-center leading-tight">
                Control<br>Escolar
              </h3>
            </div>
            <div class="border-t-2 border-white"></div>
          </div>
        </a>
        <p class="text-gray-800 text-sm leading-relaxed mt-4">
          Consulta calificaciones, horarios y expediente desde cualquier lugar
        </p>
      </div>

      <!-- Tutorías Académicas -->
      <div class="group">
        <a 
          href="/tutorias-academicas" 
          class="block relative overflow-hidden rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl"
        >
          <div 
            class="absolute inset-0 bg-cover bg-center bg-no-repeat"
            style={`background-image: url(${FondoAccesos.src})`}
          ></div>
          <div 
            class="absolute inset-0 opacity-0 dark:opacity-100 bg-cover bg-center bg-no-repeat transition-opacity duration-300"
            style={`background-image: url(${FondoAccesos_oscuro.src})`}
          ></div>
          <div class="relative p-8 h-64 flex flex-col justify-between">
            <div class="border-t-2 border-white mb-4"></div>
            <div class="flex-grow flex items-center justify-center">
              <h3 class="text-2xl font-bold text-white text-center leading-tight">
                Tutorías<br>Académicas
              </h3>
            </div>
            <div class="border-t-2 border-white"></div>
          </div>
        </a>
        <p class="text-gray-800 text-sm leading-relaxed mt-4">
          Acompañamiento personalizado con profesores expertos en cada área
        </p>
      </div>

      <!-- Trámites en Línea -->
      <div class="group">
        <a 
          href="/tramites-en-linea" 
          class="block relative overflow-hidden rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl"
        >
          <div 
            class="absolute inset-0 bg-cover bg-center bg-no-repeat"
            style={`background-image: url(${FondoAccesos.src})`}
          ></div>
          <div 
            class="absolute inset-0 opacity-0 dark:opacity-100 bg-cover bg-center bg-no-repeat transition-opacity duration-300"
            style={`background-image: url(${FondoAccesos_oscuro.src})`}
          ></div>
          <div class="relative p-8 h-64 flex flex-col justify-between">
            <div class="border-t-2 border-white mb-4"></div>
            <div class="flex-grow flex items-center justify-center">
              <h3 class="text-2xl font-bold text-white text-center leading-tight">
                Trámites en<br>Línea
              </h3>
            </div>
            <div class="border-t-2 border-white"></div>
          </div>
        </a>
        <p class="text-gray-800 text-sm leading-relaxed mt-4">
          Constancias, certificados y documentos oficiales sin filas ni esperas
        </p>
      </div>

      <!-- Credencialización -->
      <div class="group">
        <a 
          href="/credencializacion" 
          class="block relative overflow-hidden rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl"
        >
          <div 
            class="absolute inset-0 bg-cover bg-center bg-no-repeat"
            style={`background-image: url(${FondoAccesos.src})`}
          ></div>
          <div 
            class="absolute inset-0 opacity-0 dark:opacity-100 bg-cover bg-center bg-no-repeat transition-opacity duration-300"
            style={`background-image: url(${FondoAccesos_oscuro.src})`}
          ></div>
          <div class="relative p-8 h-64 flex flex-col justify-between">
            <div class="border-t-2 border-white mb-4"></div>
            <div class="flex-grow flex items-center justify-center">
              <h3 class="text-2xl font-bold text-white text-center leading-tight">
                Credencialización
              </h3>
            </div>
            <div class="border-t-2 border-white"></div>
          </div>
        </a>
        <p class="text-gray-800 text-sm leading-relaxed mt-4">
          Emisión rápida de credenciales estudiantiles y reposiciones
        </p>
      </div>

      <!-- Servicios Médicos -->
      <div class="group">
        <a 
          href="/servicios-medicos" 
          class="block relative overflow-hidden rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl"
        >
          <div 
            class="absolute inset-0 bg-cover bg-center bg-no-repeat"
            style={`background-image: url(${FondoAccesos.src})`}
          ></div>
          <div 
            class="absolute inset-0 opacity-0 dark:opacity-100 bg-cover bg-center bg-no-repeat transition-opacity duration-300"
            style={`background-image: url(${FondoAccesos_oscuro.src})`}
          ></div>
          <div class="relative p-8 h-64 flex flex-col justify-between">
            <div class="border-t-2 border-white mb-4"></div>
            <div class="flex-grow flex items-center justify-center">
              <h3 class="text-2xl font-bold text-white text-center leading-tight">
                Servicios<br>Médicos
              </h3>
            </div>
            <div class="border-t-2 border-white"></div>
          </div>
        </a>
        <p class="text-gray-800 text-sm leading-relaxed mt-4">
          Atención básica de salud en planteles con personal calificado
        </p>
      </div>

      <!-- Centro de Atención -->
      <div class="group">
        <a 
          href="/centro-de-atencion" 
          class="block relative overflow-hidden rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl"
        >
          <div 
            class="absolute inset-0 bg-cover bg-center bg-no-repeat"
            style={`background-image: url(${FondoAccesos.src})`}
          ></div>
          <div 
            class="absolute inset-0 opacity-0 dark:opacity-100 bg-cover bg-center bg-no-repeat transition-opacity duration-300"
            style={`background-image: url(${FondoAccesos_oscuro.src})`}
          ></div>
          <div class="relative p-8 h-64 flex flex-col justify-between">
            <div class="border-t-2 border-white mb-4"></div>
            <div class="flex-grow flex items-center justify-center">
              <h3 class="text-2xl font-bold text-white text-center leading-tight">
                Centro de<br>Atención
              </h3>
            </div>
            <div class="border-t-2 border-white"></div>
          </div>
        </a>
        <p class="text-gray-800 text-sm leading-relaxed mt-4">
          Un espacio seguro donde estamos siempre para ti
        </p>
      </div>

    </div>
  </div>
</section>

